// NativeAd is not registered by pinned flet_ads. BannerAd is the only rendered
// ad control and InterstitialAd is service-only, so the former NativeAd template
// helpers were unreachable and intentionally removed.
